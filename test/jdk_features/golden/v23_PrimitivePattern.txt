public class v23_PrimitivePattern {
    public v23_PrimitivePattern() {
    }
    public String classify(Object p0) {
        return switch (p0) {
            case int p4 when p4 > 0 -> "positive int: " + p4;
            case int p5 -> "int: " + p5;
            case long p6 -> "long: " + p6;
            case float p8 -> "float: " + p8;
            case double p9 -> "double: " + p9;
            case null -> "null";
            default -> "other";
        };

    }
    public double toDouble(Object p0) {
        if (p0 instanceof int p2) {
            return ((double) (p2));
        }

        if (p0 instanceof long p2) {
            return ((double) (p2));
        }

        if (p0 instanceof double p2) {
            return p2;
        }

        return 0.0;
    }
}
