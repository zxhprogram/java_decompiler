public class v19_VirtualThreads {
    public v19_VirtualThreads() {
    }
    public String describe(Object p0) {
        return switch (p0) {
            case v19_VirtualThreads$Point(int x, int y) -> "point " + x + "," + y;
            case v19_VirtualThreads$Circle p7 when p7.r() > 0.0 -> "circle r=" + p7.r();
            case v19_VirtualThreads$Circle p8 -> "circle";
            case null -> "null";
            default -> "other";
        };

    }
    public double area(v19_VirtualThreads$Shape p0) {

        return switch (p0) {
            case v19_VirtualThreads$Circle(double r) -> (r * (r * 3.141592653589793));
            case v19_VirtualThreads$Box(double w, double h) -> (h * w);
        };
    }
    public String virtualThread() throws InterruptedException {
        Thread p1 = Thread.startVirtualThread(() -> System.out.println("hi"));
        p1.join();
        return p1.toString();
    }
    private static void lambda$virtualThread$0() {
        System.out.println("hi");
    }
}
